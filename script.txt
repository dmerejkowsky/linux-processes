#

Run true
echo $?
Run false
echo $?

Run ./die
One way to suceed (exit 0)
Two ways to fail

Run ./hello
Press `ctrl-c`

Lookup `130-128` in ./signals.txt - show it's indeed INT (interrupt)

kill -s INT <pid>
kill -s KILL <pid> (KILL)

kill -s STOP <pid>
kils -s CONT <pid>

./signal-catcher
windows change
ctr-c 'are you sure'
kill -s KiLL <pid>

Uncomment signal handler for SIGKILL: show it does not work

Finall show code in `./run`
